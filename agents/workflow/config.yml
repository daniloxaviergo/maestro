# Workflow agent configuration
# Defines agent sequence and Backlog CLI command for task assignment

agents: catarina, thomas
backlog_command: backlog task edit

script_path: "./agents/workflow/script.py"
enabled: true
